<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/styles/output.css">
</head>
<body>
    <div class="max-w-4xl mx-100 p-10 border border-gray-200 rounded-md">
        <h1 class="text-4xl font-bold text-center mb-8">Welcome to Blog Website</h1>
        <a href="/create" class="bg-yellow-300 px-4 py-2 rounded">Create New Post</a>
        <div class="mt-5 ">
            <% posts.forEach(post=> { %>
                <div class="mb-8">
                    <h1 class="text-2xl font-bold mb-5">
                        <%= post.title %>
                    </h1>
                    <p>
                        <%= post.content %>
                    </p>
                    <div class="flex justify-between mt-5">
                        <div class="flex gap-5">
                            <a href="/edit/<%= post.id %>"
                                class="bg-yellow-500 px-4 py-2 text-center rounded w-18 h-10">Edit</a>
                            <form action="/delete/<%= post.id %> ?_method=DELETE" method="post"
                                onsubmit="return confirm('Are you sure you want to delete this post?')">
                                <button type="submit"
                                    class="bg-red-500 px-4 py-2 text-center rounded w-18 h-10">Delete</button>
                            </form>
                        </div>
                        <div class="flex ">
                            <img src="/imageProfile.jpg" alt="Profile Image" class="w-12 h-12 rounded-full">
                            <div class="flex items-center">
                                <p class="text-sm text-gray-600">Posted by <%= post.author %>
                                </p>
                            </div>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>

</html>